{
  "name": "{{name}}",
  "data": {
    "year": "{{year}}",
    "price": "{{price}}",
    "CPU model": "{{cpu_model}}",
    "Hard disk size": "{{hard_disk_size}}",
    "color": "{{color}}",
    "country": "{{country}}"
  }
}
